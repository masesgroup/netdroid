# This is a basic workflow to help you get started with Actions

name: CI_GENERATECLASSES

# Controls when the action will run. Triggers the workflow on push
# events but only for the master branch
on:
  workflow_dispatch:
    inputs:
      GeneratePR:
        description: 'Generate the PR at the end'
        required: true
        default: false
        type: boolean
      UseLastRepositoryBuild:
        description: 'Use latest NuGet packages from JNet'
        required: true
        default: false
        type: boolean
      UseLatestJNetReflectorSource:
        description: 'Shall use latest unpublished code of JNetReflector'
        required: true
        default: false
        type: boolean
      ContinueOnCompilationError:
        description: 'Shall continue execution in case of failure of build from latest class generation'
        required: true
        default: false
        type: boolean
        
# This workflow contains one job called "build_documentation"
jobs:
  build_classes:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    env:
      DOTNET_DbgEnableMiniDump: 1
      DOTNET_DbgMiniDumpType: 3
      DOTNET_DbgMiniDumpName: ${{ github.workspace }}/coredump.%p
      DOTNET_CreateDumpDiagnostics: 1
      DOTNET_CreateDumpVerboseDiagnostics: 1
      DOTNET_EnableCrashReport: 1
      JCOBRIDGE_LicensePath: ${{ secrets.JCOBRIDGE_ENCODED_2_6_9 }}
      JNet_Version: 2.6.9
      ANDROID_PLATFORM: android-36

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v6
        with:
          fetch-depth: '1'
          submodules: 'true'

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: |
            9.x
            10.x

      - uses: actions/checkout@v6
        if: ${{ inputs.UseLatestJNetReflectorSource == true }}
        with:
          repository: masesgroup/JNet
          path: JNet

      - name: Prebuild JNetReflector
        if: ${{ inputs.UseLatestJNetReflectorSource == true }}
        run: dotnet build --no-incremental --configuration Release JNet\src\net\JNetReflector\JNetReflector.csproj

      - name: Set up Apache Maven Central
        if: ${{ inputs.UseLatestJNetReflectorSource == true }}
        uses: actions/setup-java@v5
        with: # running setup-java again overwrites the settings.xml
          distribution: temurin
          java-version: 11
          cache: 'maven'

      - name: Create Jars of JNetReflector
        if: ${{ inputs.UseLatestJNetReflectorSource == true }}
        run: mvn "-DbinFolderName=binReflector" --file ./JNet/src/jvm/jnetreflector/pom.xml --no-transfer-progress package

      - name: Build JNetReflector
        if: ${{ inputs.UseLatestJNetReflectorSource == true }}
        run: dotnet build --no-incremental --configuration Release JNet\src\net\JNetReflector\JNetReflector.csproj

      - name: Clear C# generated files
        shell: pwsh
        run: Remove-Item ./src/net/Netdroid/Generated/* -Recurse -Force -Exclude README.md,.editorconfig

      - name: Clear Java generated files
        shell: pwsh
        run: Remove-Item ./src/jvm/netdroid/src/main/java/org/mases/netdroid/generated/* -Recurse -Force -Exclude README.md

      - name: Get last workflow run of JNet
        if: ${{ inputs.UseLastRepositoryBuild == true }}
        shell: bash
        run: echo "JNET_LAST_RUN=$(curl -sL https://api.github.com/repos/masesgroup/JNet/actions/workflows/build.yaml/runs | jq -r '.workflow_runs[0].id?')" >> $GITHUB_ENV

      - name: Download artifact
        if: ${{ inputs.UseLastRepositoryBuild == true }}
        run: gh run download ${{ env.JNET_LAST_RUN }} --name JNetNuGet --repo masesgroup/JNet --dir JNetNuGet
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: List NuGet packages downloaded
        if: ${{ inputs.UseLastRepositoryBuild == true }}
        continue-on-error: true
        run: ls ./JNetNuGet/*nupkg

      - name: Add local repo
        if: ${{ success() && inputs.UseLastRepositoryBuild == true }}
        run: |
          dotnet nuget add source --name local "${{ github.workspace }}/JNetNuGet/"
          dotnet nuget disable source nuget.org

      - name: Use latest unpublished NuGet of JNetReflector
        if: ${{ inputs.UseLastRepositoryBuild == true && inputs.UseLatestJNetReflectorSource == false }}
        run: dotnet tool update --prerelease -g MASES.JNetReflector

      - name: Download latest published version of JNetReflector
        if: ${{ inputs.UseLastRepositoryBuild == false && inputs.UseLatestJNetReflectorSource == false }}
        run: dotnet tool update --version ${{ env.JNet_Version }} -g MASES.JNetReflector

      - name: Create Jars of JNetReflector
        if: ${{ inputs.UseLatestJNetReflectorSource == true }}
        run: mvn "-DbinFolderName=binReflector" --file ./JNet/src/jvm/jnet/pom.xml --no-transfer-progress package

      - name: Build JNetReflector
        if: ${{ inputs.UseLatestJNetReflectorSource == true }}
        run: dotnet build --no-incremental --configuration Release JNet/src/net/JNetReflector/JNetReflector.csproj

      - name: Download latest published version of JNetReflector
        if: ${{ inputs.UseLatestJNetReflectorSource == false }}
        run: dotnet tool update --version ${{ env.JNet_Version }} -g MASES.JNetReflector

      - name: Set up Apache Maven Central
        uses: actions/setup-java@v5
        with: # running setup-java again overwrites the settings.xml
          distribution: temurin
          java-version: 17
          cache: 'maven'

      - name: Create classes with unpublished JNetReflector
        if: ${{ inputs.UseLatestJNetReflectorSource == true }}
        run: |
          cd JNet/binReflector/net8.0
          dotnet MASES.JNetReflector.dll -TraceLevel 1 -TraceTo $GITHUB_WORKSPACE/JNetReflector.txt -CopyrightFile $GITHUB_WORKSPACE/src/copyright.txt -OriginRootPath $ANDROID_HOME/platforms/${{ env.ANDROID_PLATFORM }} -DestinationRootPath $GITHUB_WORKSPACE/src/ -ConfigurationFile $GITHUB_WORKSPACE/src/configuration.json

      - name: Create classes with latest published version of JNetReflector
        if: ${{ inputs.UseLatestJNetReflectorSource == false }}
        run: jnetreflector -TraceLevel 1 -TraceTo $GITHUB_WORKSPACE/JNetReflector.txt -CopyrightFile $GITHUB_WORKSPACE/src/copyright.txt -OriginRootPath $ANDROID_HOME/platforms/${{ env.ANDROID_PLATFORM }} -DestinationRootPath $GITHUB_WORKSPACE/src/ -ConfigurationFile $GITHUB_WORKSPACE/src/configuration.json

      - uses: actions/upload-artifact@v7
        with:
          name: JNetReflector report
          path: ./JNetReflector.txt

      - name: Enable again nuget.org disabling local
        if: ${{ success() && inputs.UseLastRepositoryBuild == true }}
        run: |
          dotnet nuget disable source local
          dotnet nuget enable source nuget.org

      - name: Pre compile
        continue-on-error: ${{ inputs.ContinueOnCompilationError == true }}
        run: dotnet build --no-incremental --configuration Release /p:Platform="Any CPU" /p:NoWarn="0108%3B1030%3B0618" src/net/Netdroid/Netdroid.csproj
        env:
          GITHUB_SIMPLIFIED_GENERATION: true

      - name: Set up Apache Maven Central
        uses: actions/setup-java@v5
        with: # running setup-java again overwrites the settings.xml
          distribution: temurin
          java-version: 11 # revert to 11 following https://stackoverflow.com/questions/13113142/gmaven-build-fails-with-an-api-incompatibility-was-encountered
          cache: 'maven'

      - name: Try compilation of Jars
        continue-on-error: ${{ inputs.ContinueOnCompilationError == true }}
        run: mvn "-Dandroid.platform=${{ env.ANDROID_PLATFORM }}" --file ./src/jvm/netdroid/pom.xml --no-transfer-progress package

      - name: Try compilation of C#
        continue-on-error: ${{ inputs.ContinueOnCompilationError == true }}
        run: dotnet build --no-incremental --configuration Release /p:Platform="Any CPU" /p:NoWarn="0108%3B1030%3B0618" src/net/Netdroid.sln

      - name: Extract commit SHA
        if: ${{ inputs.GeneratePR == true && inputs.UseLatestJNetReflectorSource == false }}
        run: |
          echo "GITHUB_COMMIT_MESSAGE=$(echo $GITHUB_SHA | cut -c 1-7)" >> $GITHUB_ENV
        shell: bash

      - name: Request a PR to commit changes made with unpublished version of JNetReflector
        if: ${{ inputs.GeneratePR == true && (inputs.UseLatestJNetReflectorSource == true || inputs.UseLastRepositoryBuild == true) }}
        uses: peter-evans/create-pull-request@v8
        with:
          branch-suffix: short-commit-hash
          add-paths: src/*.*
          commit-message: Update generated classes using last unpublished version of JNetReflector after commit ${{ env.GITHUB_COMMIT_MESSAGE }}
          title: Update generated classes using last unpublished version of JNetReflector after commit ${{ env.GITHUB_COMMIT_MESSAGE }}
          body: |
            Automated changes by GitHub action, fix #2
          reviewers: masesdevelopers
          assignees: masesdevelopers
          labels: java, .NET, enhancement 

      - name: Request a PR to commit changes
        if: ${{ inputs.GeneratePR == true && inputs.UseLatestJNetReflectorSource == false && inputs.UseLastRepositoryBuild == false }}
        uses: peter-evans/create-pull-request@v8
        with:
          branch-suffix: short-commit-hash
          add-paths: src/*.*
          commit-message: Update generated classes using last public version of JNetReflector after commit ${{ env.GITHUB_COMMIT_MESSAGE }}
          title: Update generated classes using last public version of JNetReflector after commit ${{ env.GITHUB_COMMIT_MESSAGE }}
          body: |
            Automated changes by GitHub action, fix #2
          reviewers: masesdevelopers
          assignees: masesdevelopers
          labels: java, .NET, enhancement 
